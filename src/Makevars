PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
LAPACK_LIBS = -llapack
BLAS_LIBS = -lblas

# Remove non-portable compilation flags
PKG_CXXFLAGS = $(shell echo "$(R_XTRA_CXXFLAGS)" | sed 's/-Wdate-time//g' | sed 's/-Wformat//g')
